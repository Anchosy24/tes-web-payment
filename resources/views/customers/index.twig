{% extends "layouts/home.twig" %}

{% block title %}Customer Home{% endblock %}

{% block content %}
<div class="card">
    <div class="card-body">
        <h3>Customer Home</h3>
        <p>Selamat datang, {{ session.user.username }}!</p>
        <a href="{{ path_for('logout') }}" class="btn btn-danger">Logout</a>
    </div>
</div>
{% endblock %}