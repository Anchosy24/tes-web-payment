<nav id="sidebar" class="d-flex flex-column p-3">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <i class="bi bi-play-circle-fill" style="font-size: 2rem;"></i>
            <h5 class="d-inline ms-2 mb-0 fw-bold">Admin Panel</h5>
        </div>
        <button class="btn btn-sm btn-light d-lg-none" id="closeSidebar">
            <i class="fas fa-times"></i>
        </button>
    </div>

    <hr class="border-light opacity-25">

    <ul class="nav nav-pills flex-column mb-auto">
        <li class="nav-item">
            <a href="{{ path_for('admin.dashboard') }}" class="nav-link text-white">
                <i class="bi bi-speedometer2 me-2"></i> Dashboard
            </a>
        </li>
        <li class="nav-item">
            <a href="{{ path_for('admin.orders') }}" class="nav-link text-white">
                <i class="bi bi-box-seam me-2"></i> Orders
            </a>
        </li>
        <li class="nav-item">
            <a href="{{ path_for('admin.payment') }}" class="nav-link text-white">
                <i class="bi bi-clock-history me-2"></i> Payment History
            </a>
        </li>
        <li class="nav-item">
            <a href="{{ path_for('admin.users') }}" class="nav-link text-white">
                <i class="bi bi-people me-2"></i> Users
            </a>
        </li>
    </ul>

    <!-- User Info & Logout -->
    <div class="mt-auto pt-3 border-top border-light border-opacity-25">
        <div class="d-flex align-items-center mb-3 text-white">
            <div class="bg-light rounded-circle p-2 me-2">
                <i class="bi bi-person-fill text-primary"></i>
            </div>
            <div>
                <small class="d-block opacity-75">Logged in as</small>
                <strong>{{ session.user.username ?? 'Admin' }}</strong>
            </div>
        </div>
        <a href="{{ path_for('logout') }}" class="btn btn-light w-100" onclick="return confirmLogout(event)">
            <i class="bi bi-box-arrow-right me-2"></i> Logout
        </a>
    </div>
</nav>

<script>
function confirmLogout(e) {
    e.preventDefault();
    Swal.fire({
        title: 'Logout',
        text: 'Apakah Anda yakin ingin keluar?',
        icon: 'question',
        showCancelButton: true,
        confirmButtonColor: '#dc3545',
        cancelButtonColor: '#6c757d',
        confirmButtonText: 'Ya, Logout',
        cancelButtonText: 'Batal'
    }).then((result) => {
        if (result.isConfirmed) {
            window.location.href = '{{ path_for('logout') }}';
        }
    });
    return false;
}
</script>